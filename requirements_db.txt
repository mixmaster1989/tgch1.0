# Зависимости для работы с базами данных
psycopg2-binary==2.9.9
redis==5.0.1

# Дополнительные зависимости для работы с данными
pandas==2.1.4
numpy==1.24.3
sqlalchemy==2.0.23

# Для работы с JSON в PostgreSQL (встроено в psycopg2)
# psycopg2-json==0.1.0 - НЕ СУЩЕСТВУЕТ

# Для мониторинга производительности (устанавливается отдельно в PostgreSQL)
# pg_stat_statements==1.10 - НЕ PYTHON ПАКЕТ

# Для бэкапов (системная утилита)
# pg_dump==15.5 - НЕ PYTHON ПАКЕТ 